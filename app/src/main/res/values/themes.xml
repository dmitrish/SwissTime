<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SwissTime" parent="android:Theme.Material.Light.NoActionBar"/>
    <style name="Theme.SplashScreen" parent="android:Theme.Material.Light.NoActionBar"/>

    <style name="Theme.PostSplash" parent="android:Theme.Material.Light.NoActionBar" >
        <item name="android:windowActionBar">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color. -->
        <item name="windowSplashScreenBackground">@color/dark_navy</item>

        <!-- Use our splash screen icon as the drawable. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_splash_screen</item>

        <!-- Set the theme of the Activity that follows the splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.PostSplash</item>

        <item name="windowSplashScreenAnimationDuration">2000</item>
    </style>
</resources>
